'use strict';

const Homey = require('homey');

module.exports = [

  // define API calls here

  /*
	{
		description: 'Return list of known Espurna devices',
		method: 'GET',
		path: '/devices/',
		fn: (args, callback) => {
			// logic to get data from Homey.app.var and/or Homey.app.var.db (?)
      // if true return callback(null, Homey.app.nestAccount.db.getAuth());
			// else return callback('No nest account found');
		},
	}
  */

]; // module.exports
